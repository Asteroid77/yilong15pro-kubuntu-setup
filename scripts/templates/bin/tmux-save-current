#!/usr/bin/env bash
set -euo pipefail

resurrect_save="$HOME/.tmux/plugins/tmux-resurrect/scripts/save.sh"
if [[ ! -x "$resurrect_save" ]]; then
  echo "tmux-resurrect save script not found: $resurrect_save" >&2
  exit 1
fi

"$resurrect_save" quiet >/dev/null 2>&1
